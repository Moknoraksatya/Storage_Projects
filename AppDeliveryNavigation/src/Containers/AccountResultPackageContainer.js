import {connect} from 'react-redux'
import AccountResultPackage from '../Screens/AccountResultPackage'
const mapStateToProps = state =>({

})
const mapDispatchToProps ={

}
export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(AccountResultPackage)